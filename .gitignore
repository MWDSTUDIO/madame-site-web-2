# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Node (no build step required for this static site, but keep clean if tooling is added)
node_modules/
npm-debug.log*

# Local env / secrets
.env
.env.*

# Netlify local folder
.netlify/

# Large source media that should not live in git (deploy via Netlify Large Media or host)
assets/video/*.mp4
assets/video/*.webm
assets/audio/*.mp3

# Impeccable live-mode scratch
.impeccable/live/

# Committed exception: the enquiry-form reel (compressed to web weight)
!assets/video/enquiries.mp4
